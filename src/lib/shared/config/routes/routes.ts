import { Routes } from "./types";

export const routes: Routes = {
  home: {
    label: "Home",
    path : () => "/",
    icon : null,
  },
  resume: {
    label: "Resume",
    path : () => "/resume",
    icon : null,
  },
  dojo: {
    label: "Dojo",
    path : () => "/dojo",
    icon : null,
  },
};
